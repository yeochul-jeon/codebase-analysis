# 작업 로그 — 2026-04-19

| 항목 | 내용 |
|:---|:---|
| 세션 날짜 | 2026-04-19 |
| 주제 | 경량 코드 분석 플랫폼 MVP 초기 기획 · 문서 정리 |
| 상태 | 세션 4 완료 |

---

## 세션 요약

`codebase-memory-sync/docs/lightweight/01-design.md`를 검토해 경량 MVP 설계를 흡수하고, 본 레포(`codebase-analysis`)에 ADR·OPEN-QUESTIONS·ARCHITECTURE·PRD 초기 문서 세트를 구축. OQ-001(언어 스택)에 대한 권고안 수립 후 Node.js + Hono로 방향 잠정 확정. `codeatlas` 프로젝트를 코드 이식 레퍼런스로 활용 결정.

---

## 태스크 현황

| Task | 내용 | 상태 |
|:---|:---|:---|
| 1.1 | `01-design.md` 및 CMS `ADR.md` 검토 | ✅ |
| 1.2 | `docs/ADR.md` 초기 12개 결정 기록 | ✅ |
| 1.3 | `docs/OPEN-QUESTIONS.md` 6개 미결 항목 정리 | ✅ |
| 1.4 | OQ-001 언어 스택 권고안 작성 (`/Users/cjenm/.claude/plans/oq-001-snoopy-swan.md`) | ✅ |
| 1.5 | `codeatlas` 프로젝트 분석 · 재사용 평가 | ✅ |
| 1.6 | `docs/ARCHITECTURE.md` 작성 | ✅ |
| 1.7 | `docs/PRD.md` 작성 | ✅ |
| 1.8 | `docs/FUTURE-TASKS.md` 작성 (LanceDB · 그래프 DB 확장 태스크) | ✅ |
| 1.9 | ADR-013 (Node.js + Hono 확정) 추가 | ✅ |
| 1.10 | ADR-014 (codeatlas 레퍼런스 선택 이식) 추가 | ✅ |
| 2.1 | OQ-002 배포 변형 결정 — Variant A 우선, B 병행 (ADR-015) | ✅ |
| 2.2 | OQ-003 tree-sitter grammar — TS/Java 2종 확정 (ADR-016, Kotlin 보류·FT-004) | ✅ |
| 2.3 | FT-003 Python extractor FUTURE-TASKS에 추가 | ✅ |
| 2.4 | FT-004 Kotlin extractor FUTURE-TASKS에 추가 (codeatlas stub 확인 후 이연) | ✅ |
| 2.5 | ADR-016 수정 (TS+Java 2종, Kotlin 제거), 관련 문서 동기화 | ✅ |
| 3.1 | Corepack 활성화 + pnpm workspace 스캐폴딩 | ✅ |
| 3.2 | `packages/cli/src/parser/parser.ts` — codeatlas 32K 이식 | ✅ |
| 3.3 | `packages/shared/src/types.ts` — SymbolBase 은퇴 · 4개 타입 추가 | ✅ |
| 3.4 | `packages/cli/src/extractors/typescript.ts` — js-extractor 이식 (JS·TS·TSX) | ✅ |
| 3.5 | `packages/cli/src/extractors/java.ts` — java-extractor 이식 | ✅ |
| 3.6 | `packages/cli/src/extractors/index.ts` — barrel export | ✅ |
| 3.7 | TS extractor smoke 테스트 (20 cases, all pass) | ✅ |
| 3.8 | Java extractor smoke 테스트 (20 cases, all pass) | ✅ |
| 3.9 | `parser/__tests__/smoke.ts` import 경로 버그 수정 (`./parser.js` → `../parser.js`) | ✅ |
| 3.10 | `pnpm typecheck` · `pnpm build` 전체 통과 확인 | ✅ |
| 4.1 | ADR-017 (SQLite 마이그레이션 전략) 문서화 | ✅ |
| 4.2 | ADR-018 (better-sqlite3 선택) 문서화 | ✅ |
| 4.3 | ADR-019 (adm-zip 선택) 문서화 | ✅ |
| 4.4 | ADR-020 (`analyze push` HTTP 전용, CLI 직접 DB 쓰기 금지) 문서화 | ✅ |
| 4.5 | `better-sqlite3` · `adm-zip` 의존성 추가 | ✅ |
| 4.6 | `DbAdapter` 인터페이스 (`packages/server/src/storage/db/types.ts`) | ✅ |
| 4.7 | DB 마이그레이션 SQL 파일 001~003 (FTS5 포함) | ✅ |
| 4.8 | 마이그레이션 러너 (`packages/server/src/storage/db/migrate.ts`) | ✅ |
| 4.9 | `SqliteAdapter` 구현 (`packages/server/src/storage/db/sqlite.ts`) | ✅ |
| 4.10 | `BlobAdapter` 인터페이스 + FS 어댑터 (`packages/server/src/storage/blob/`) | ✅ |
| 4.11 | storage smoke 테스트 (`packages/server/src/storage/__tests__/smoke.ts`, 14/14 pass) + `pnpm typecheck` · `pnpm build` 전체 통과 | ✅ |
| ~~4.12~~ | ~~패커 (`packages/cli/src/packer/`) + `analyze .` · `analyze push` CLI~~ | → Session 5 이연 |

---

## 의사결정 기록 (ADR)

| ADR | 결정 | 근거 |
|:---|:---|:---|
| ADR-001~012 | 설계서 흡수 결정 (어댑터 분리, tree-sitter 범위, zip blob, Bearer token 등) | `01-design.md` 전면 수용 + CMS 리뷰 반영분 채택 |
| ADR-013 | Node.js 22 LTS + Hono + TypeScript 확정 | MCP SDK 정합성 · AWS cold-start 우위 · CLI/서버 단일 런타임 |
| ADR-014 | codeatlas는 런타임 의존 X, MIT 라이선스 하에 코드 레퍼런스로 선택 이식 | 1인 저자 · 6일차 프로젝트 · 전역 SQLite 하드코딩 등 블로커. 그러나 tree-sitter 추출기·MCP tool 시그니처는 가치 있음 |
| ADR-015 | Variant A 우선 구현, B 병행 (skeleton 유지) | ADR-001/005 정합 · AWS 없이 즉시 dogfooding 가능 |
| ADR-016 | grammar 초기 2종 TS/Java (Kotlin 보류) | codeatlas kotlin-extractor 21L stub·throw 확인 → FT-004 이연. Python은 FT-003 |
| ADR-017 | SQLite 마이그레이션 — 번호 기반 SQL 파일 + 자체 러너 (OQ-004 Variant A 해결) | `node-pg-migrate`는 PG 전용. MVP SQLite에는 외부 라이브러리 불필요 |
| ADR-018 | SQLite 드라이버 — `better-sqlite3` 선택 | 동기 API·단일 프로세스 모델(ADR-001) 정합·TypeScript 타입 내장 |
| ADR-019 | zip 라이브러리 — `adm-zip` 선택 | CLI(write) · 서버(random-access read) 단일 라이브러리로 처리 가능 |
| ADR-020 | `analyze push`는 HTTP 전용, CLI 직접 DB 쓰기 금지 | ADR-004 정합 — 두 변형 CLI 동일 경로. REST API는 Session 5, `analyze push`도 Session 5 |

---

## 미결 항목

| 항목 | 내용 | 우선순위 |
|:---|:---|:---|
| OQ-004 | DB 마이그레이션 툴 — Variant A 해결 (번호 기반 SQL + 자체 러너, ADR-017). Variant B는 `node-pg-migrate` 이연 | ✅ (A) / P2 (B) |
| OQ-005 | 웹 UI 스택 (vanilla HTML + Hono 정적 서빙 유력) | P2 |
| OQ-006 | 레포 구조 — 별도 레포 기정 사실 (확인만 필요) | P3 |
| FT-001 | 시맨틱 검색(LanceDB/pgvector) 도입 트리거 시점 | 확장 |
| FT-002 | 그래프 DB(Kuzu/Neo4j/PG CTE) 도입 트리거 시점 | 확장 |

---

## 세션 2 요약

codeatlas `kotlin-extractor.ts` 조사 결과 21줄 스텁·런타임 throw 확인. ADR-016을 TS+Java 2종으로 수정하고, Kotlin은 FT-004로 이연. 이후 Corepack(pnpm) 설치, pnpm workspace 스캐폴딩(`packages/server, cli, shared`), `packages/cli/src/parser/parser.ts` 이식(32K 스트리밍 우회 포함) 완료. `/healthz` healthcheck 200 OK 확인.

---

## 세션 3 요약

`packages/shared/src/types.ts`를 `ExtractedSymbol`·`ExtractionResult`·`ExtractedDependency`·`ExtractedRef` 4개 타입으로 재작성(SymbolBase 은퇴). `packages/cli/src/extractors/typescript.ts`(codeatlas js-extractor 638L 이식)·`java.ts`(244L 이식) 신설. 각 extractor smoke 테스트 20 cases 전량 통과. `pnpm typecheck`·`pnpm build` 3패키지 전부 통과. 기존 parser smoke 회귀 없음. `parser/__tests__/smoke.ts`의 pre-existing import 경로 버그(`./parser.js` → `../parser.js`) 함께 수정.

## 세션 4 요약

ADR-017~020 문서화(마이그레이션 전략·SQLite 드라이버·zip 라이브러리·HTTP 전용 push). `better-sqlite3`·`adm-zip` 의존성 설치 및 네이티브 빌드 승인. `DbAdapter` 인터페이스·`SqliteAdapter`·번호 기반 SQL 마이그레이션 3개(초기 스키마·B-tree 인덱스·FTS5 트리거)·마이그레이션 러너·`BlobAdapter`·`FsBlobAdapter` 구현 완료. FTS5 update 트리거에서 `old.*`/`new.*` 사용 정확성 확인. storage smoke 14/14 pass. `pnpm typecheck`·`pnpm build` 전체 통과. `analyze push` 및 패커는 ADR-020(HTTP 전용) 결정에 따라 Session 5로 이연.

---

## 다음 세션(Session 5) 시작 지점

- **가장 먼저 할 것**: 패커 (`packages/cli/src/packer/index.ts`) — `_nodeId → symbol_key(ADR-008)` 맵 구성, `index.json` + `source.zip` 생성
- **그다음**: `analyze .` CLI 서브커맨드 (walk 디렉터리, 추출, 패킹)
- **그다음**: REST API 엔드포인트 (`POST /v1/repos/:name/indexes`, upload, `PATCH /v1/indexes/:id`) — Session 4 어댑터 위에 Hono 라우터 배선
- **그다음**: `analyze push` CLI 서브커맨드 (HTTP 전용, ADR-020)
- **의존성/전제 조건**:
  - Session 4에서 `DbAdapter`·`BlobAdapter` 인터페이스 확정 완료
  - `_nodeId → symbol_key` 해시: `sha256(repo_id + commit_sha + file_path + name + kind + start_line)` hex, 64자 (ADR-008)
  - CLI는 `better-sqlite3` 직접 의존 금지 — HTTP 경로만 사용 (ADR-020)
  - `symbols_au` FTS update 트리거는 현재 dormant(`INSERT OR IGNORE` write path만 존재). `UPDATE symbols` 경로 추가 시 update-name 검색 회귀 테스트 필수
