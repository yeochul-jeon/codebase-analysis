# HANDOFF — 2026-04-20

> 홀드 전 인계 문서. 이 파일 하나만 읽으면 현재 상태·다음 액션·재개 절차를 즉시 파악할 수 있다.

---

## 요약

MVP부터 P3 Web UI 고도화까지 기능 구현을 완료하고, 이번 세션(2026-04-20)에서 Claude Code CLI MCP 설정 가이드 추가·세션 Q&A 정리까지 문서화 라운드를 마무리했다. 당분간 다른 작업으로 인해 홀드. **재개 시 첫 액션은 FT-005 후속 작업(Variant B CI 스크립트 + 실레포 dogfooding)**이다.

---

## 완료된 작업

### 커밋 타임라인

| 커밋 | 내용 |
|---|---|
| `067cb18` | MVP 초기 구현 — extractor + storage layer (Session 1~4) |
| `fdc5667` | E2E dogfooding — FTS5 경로 오염 수정, stale `.js` 정리 (Session 7) |
| `49d2648` | REST API · MCP · Web UI · Docker · 테스트 표준화 (Session 5~6·8) |
| `39dae6a` | P2 Variant B Contract MVP + CLI·문서 개선 (Session 9~11) |
| `0245d64` | P3 Web UI 고도화 — 파일 개요·References 링크화·Prism 시그니처·Cache-Control |
| `56227ce` | 초보자용 문서 재편 — USAGE·TROUBLESHOOTING 신규 + 기존 확장 |
| `d062526` | 팀 소개 데모 스크립트 및 TEAM-INTRO 문서 추가 |
| `4609cdd` | Claude Code CLI MCP 등록 가이드 추가 (USAGE.md §4, TEAM-INTRO.md 사전 설정 보강) |
| `dd8a25f` | 2026-04-20 세션 가이드 문서 추가 (`docs/SESSION-GUIDE-20260420.md`) |

### 구현 완료 기능

- **저장소**: SQLite + FS(기본) · PG + S3(Variant B) — env 스위칭으로 전환 가능
- **CLI**: `analyze`(tree-sitter 파싱 → `.codebase-analysis/`) + `push`(4-step HTTP 업로드)
- **Server**: Hono REST API (`/v1/search`, `/v1/symbols`, `/v1/repos`, `/v1/indexes`)
- **MCP Server**: tool 4종 (`search_symbols`, `get_symbol_body`, `get_references`, `get_file_overview`)
- **Web UI**: 심볼 검색·상세·파일 개요 3페이지, Prism 시그니처 하이라이팅, References 링크화
- **Contract Tests**: `RUN_VARIANT_B=1` 게이트 — 15 checks (11 db + 4 blob)
- **지원 언어**: TypeScript · JavaScript · Java

### 2026-04-20 세션 Q&A 요약

→ 자세한 내용은 [`docs/SESSION-GUIDE-20260420.md`](SESSION-GUIDE-20260420.md) 참조

- Claude Code CLI `.mcp.json` 설정 가이드 → USAGE.md §4, TEAM-INTRO.md 사전 설정에 반영
- PostgreSQL · S3 교체 가능 여부 → **이미 구현됨** (`factory.ts`에서 env 변수로 전환)
- analyze → push → MCP query 전체 플로우 확인
- 처음 인덱싱 절차 (`ANALYZE_UPLOAD_TOKEN`, `ANALYZE_SERVER_URL` 설정 후 analyze + push)

---

## 레포 상태 스냅샷 (2026-04-20 기준)

- **Branch**: `main`, 원격과 동기화 완료 (`dd8a25f`)
- **Tracked 변경**: `tsconfig.tsbuildinfo` 3개 — 빌드 아티팩트, 커밋 불필요
- **Untracked**:
  - `.mcp.json` — 로컬 환경 설정. 팀원 환경에 따라 경로가 다르므로 `.gitignore` 처리 또는 커밋 여부 판단 필요
  - `packages/server/src/**/*.d.ts`, `*.js.map` — 빌드 산출물. `.gitignore` 보강 검토

---

## 다음 진행 사항

상세 범위·트리거·공수는 [`docs/FUTURE-TASKS.md`](FUTURE-TASKS.md) 참조.

| 우선순위 | ID | 내용 | 예상 공수 | 트리거 조건 |
|---|---|---|---|---|
| 1 | FT-005 후속 | CI에서 PG/S3 주입 스크립트 + Variant B 실레포 dogfooding E2E | 1~2일 | 즉시 착수 가능 |
| 2 | FT-006 | 성능 기준선 벤치마크 (`scripts/bench.ts`) | 2~3일 | 레포 10개 이상 실운영 시 또는 latency 불만 시 |
| 3 | FT-004 | Kotlin extractor | 2~3일 | Android/JVM 레포 색인 수요 발생 시 |
| 4 | FT-007 | 검색 계약 확장 (`path=` 파라미터, FTS AND/OR) | 1~2일 | OQ-009 Option B 결정 시 |
| 5 | FT-003 | Python extractor | 2~3일 | Python 레포 색인 수요 발생 시 |
| 6 | FT-001 | 시맨틱 검색 (벡터 기반) | 2주 | 자연어 쿼리 수요 · 심볼 50만+ · FTS 품질 피드백 |
| 7 | FT-002 | 그래프 DB 기반 관계 쿼리 | 3주+ | 멀티홉 CTE p99 > 2s · 관계 레코드 500만+ |
| 8 | FT-008 | 감사 로그 · 토큰 회전 | 1~2일 | 사외망 또는 다중 팀 운영 수요 |

---

## 재개 체크리스트

아래 명령을 순서대로 실행하면 바로 작업 가능한 상태가 된다.

```bash
# 1. 최신 코드 확인
cd /Users/cjenm/cjenm/platform/codebase-analysis
git status && git pull

# 2. 서버 기동
cp docker/.env.example docker/.env   # 최초 1회 (이미 있으면 skip)
docker compose -f docker/docker-compose.yml up --build -d

# 3. MCP 서버 빌드
pnpm -F @codebase-analysis/mcp-server build

# 4. dogfooding 인덱스 갱신
export ANALYZE_UPLOAD_TOKEN=demo-secret-token
export ANALYZE_SERVER_URL=http://localhost:3000
pnpm -F @codebase-analysis/cli dev -- analyze .
pnpm -F @codebase-analysis/cli dev -- push

# 5. MCP 연결 확인
claude mcp list   # codebase-analysis 항목 확인

# 6. 다음 태스크 확인
cat docs/FUTURE-TASKS.md
```

---

## 참조

| 문서 | 용도 |
|---|---|
| [`docs/USAGE.md`](USAGE.md) | CLI·Web UI·MCP·API 사용 가이드 |
| [`docs/GETTING-STARTED.md`](GETTING-STARTED.md) | 설치·기동 |
| [`docs/TROUBLESHOOTING.md`](TROUBLESHOOTING.md) | 문제 해결 체크리스트 |
| [`docs/ARCHITECTURE.md`](ARCHITECTURE.md) | 아키텍처 개요 |
| [`docs/ARCHITECTURE-RECONCILED.md`](ARCHITECTURE-RECONCILED.md) | 최종 정합 설계 |
| [`docs/FUTURE-TASKS.md`](FUTURE-TASKS.md) | 추후 태스크 목록 (상세) |
| [`docs/SESSION-GUIDE-20260420.md`](SESSION-GUIDE-20260420.md) | 직전 세션 Q&A 정리 |
| [`docs/work-log-20260419.md`](work-log-20260419.md) | 이전 작업 로그 |
| [`docs/TEAM-INTRO.md`](TEAM-INTRO.md) | 팀 공유용 소개 자료 |
| [`docs/ADR.md`](ADR.md) | 설계 결정 기록 |
