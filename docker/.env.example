# Upload authentication token (ADR-007).
# Used by CI pipelines and `analyze push` CLI.
# Copy this file to docker/.env and set a strong random value.
ANALYZE_UPLOAD_TOKEN=change-me-to-a-strong-random-string
