---
## [15:07] Session 5 — Packer · analyze . · REST API · analyze push 구현 완료

**완료한 작업:**
- `ExtractedRef`에 `line` 필드 추가 (1-based), `PackedSymbol`, `PackedOccurrence`, `PackedIndex` 인터페이스 신규 추가
- Java extractor + TS extractor의 `result.refs.push` 전 호출부에 `line` 필드 추가
- 패커 4개 파일 구현: `symbol-key.ts`, `resolve.ts`, `zip.ts`, `index.ts` (packer smoke 39/39 pass)
- Walker `collect.ts` — readdirSync 재귀, 하드코딩 skip set (node_modules 등 7종)
- `analyze .` CLI 명령어 구현 (`commands/analyze.ts`)
- `analyze push` CLI 명령어 구현 (`commands/push.ts`, Node 22 내장 fetch 사용)
- `packages/cli/src/index.ts` — analyze + push 명령어 등록
- `DbAdapter`에 `getIndexById`, `resetIndexToUploading` 추가, `SqliteAdapter` 구현
- zod `packedIndexSchema` 작성 (`schemas/packed-index.ts`)
- `requireBearer` 미들웨어 — empty token → 401, 쓰기 전용 적용
- `createIndexesRouter` — POST/PUT/PUT/PATCH 엔드포인트 4개 (REST smoke 20/20 pass)
- `createApp(deps)` DI factory (`app.ts`), `dev.ts` 어댑터 주입 방식으로 재구성
- ADR-008 clarification note 추가 (repo_id → repo_name 변경 이유 기록)
- `pnpm typecheck && pnpm build` 전 패키지 통과, 기존 smoke 회귀 없음

**변경된 파일:**
- `packages/shared/src/types.ts` — ExtractedRef.line + 3개 신규 인터페이스
- `packages/cli/src/extractors/java.ts` — refs에 line 필드
- `packages/cli/src/extractors/typescript.ts` — refs에 line 필드 (5곳)
- `packages/cli/src/index.ts` — analyze/push 명령어 등록
- `packages/server/src/storage/db/types.ts` — getIndexById, resetIndexToUploading 추가
- `packages/server/src/storage/db/sqlite.ts` — 위 두 메서드 구현
- `packages/server/src/index.ts` — createApp re-export로 단순화
- `packages/server/src/dev.ts` — DI factory 방식으로 재구성
- `docs/ADR.md` — ADR-008 clarification note 추가

**신규 파일:**
- `packages/cli/src/packer/{symbol-key,resolve,zip,index}.ts`
- `packages/cli/src/packer/__tests__/smoke.ts`
- `packages/cli/src/walker/collect.ts`
- `packages/cli/src/commands/{analyze,push}.ts`
- `packages/server/src/app.ts`
- `packages/server/src/middleware/bearer.ts`
- `packages/server/src/schemas/packed-index.ts`
- `packages/server/src/routes/{health,indexes}.ts`
- `packages/server/src/routes/__tests__/smoke.ts`

**다음 단계 (Session 6):**
- 실환경 dogfooding: `analyze . && analyze push` end-to-end 검증
- MCP server 읽기 전용 tool 4개 구현 (`search_symbols`, `get_symbol_body`, `get_references`, `get_file_overview`)
- GET REST API (`/v1/search`, `/v1/symbols/:key`, `/v1/symbols/:key/body`, `/v1/repos/:name/files`)
- 최소 웹 UI (OQ-005: vanilla HTML + Hono 정적 서빙)

**참조:**
- ref: ADR-020 — analyze push HTTP 전용 (better-sqlite3 직접 의존 금지)
- ref: ADR-008 — symbol_key = sha256(repo_name + commit_sha + …) NUL 구분
- ref: ADR-010 — 멱등 업로드 (ready → 409, uploading/failed → reset)
- ref: ADR-007 — 정적 Bearer token 인증, 쓰기 엔드포인트에만 적용
---
## [Session 6] Dogfooding + GET REST API 완료

**완료한 작업:**
- Dogfooding: `analyze . && analyze push` 실환경 end-to-end 검증 (352 symbols / 688 occurrences / 29 files, status=ready)
- `DbAdapter`에 읽기 전용 메서드 3개 추가: `getRepoByName`, `getRepoHead`, `getFilesByIndex`
- `SqliteAdapter` 구현: `getRepoByName`(SELECT 단순 조회), `getRepoHead`(repo_head 테이블), `getFilesByIndex`(DISTINCT file_path)
- `createReadsRouter(db, blob)` 신규 — 4개 GET 엔드포인트 (인증 없음, ADR-007)
- `app.ts`에 reads 라우터 등록
- Smoke 테스트 20 → 34 checks (GET 14개 추가), 34/34 pass
- 수동 curl 검증 완료: search, symbol, body, files, 에러 케이스 모두 정상

**변경된 파일:**
- `packages/server/src/storage/db/types.ts` — `getRepoByName`, `getRepoHead`, `getFilesByIndex` 추가
- `packages/server/src/storage/db/sqlite.ts` — 위 3개 구현
- `packages/server/src/app.ts` — `createReadsRouter` import + 등록
- `packages/server/src/routes/__tests__/smoke.ts` — GET 시나리오 14개 추가

**신규 파일:**
- `packages/server/src/routes/reads.ts` — `createReadsRouter`, `resolveIndex` 헬퍼

**엔드포인트 목록 (GET, 인증 불필요):**
- `GET /v1/search?q=&repo=&commit=&limit=` — FTS5 prefix 검색
- `GET /v1/symbols/:key` — 64-hex key로 심볼 조회
- `GET /v1/symbols/:key/body` — zip에서 start_line..end_line 슬라이스
- `GET /v1/repos/:name/files?commit=` — DISTINCT file_path 목록

**다음 단계 (Session 7):**
- MCP server 읽기 전용 tool 4개: `search_symbols`, `get_symbol_body`, `get_references`, `get_file_overview`
- 최소 웹 UI (OQ-005: vanilla HTML + Hono 정적 서빙)

**참조:**
- ADR-007 읽기 인증 없음, ADR-008 symbol_key 64-hex, ADR-009 resolver(commit→branch→default), ADR-010 ready만 서빙, ADR-012 FTS5 prefix, ADR-019 adm-zip body 슬라이스
---
## [Session 7] MCP Server 구현 완료

**완료한 작업:**
- `GET /v1/symbols/:key/references` REST 엔드포인트 추가 (기존 `getOccurrences` 재사용)
- `GET /v1/repos/:name/file-symbols?path=` REST 엔드포인트 추가 (기존 `getFileSymbols` 재사용)
- Server smoke 34 → 44 checks (신규 6건 추가), 44/44 pass
- `packages/mcp-server` 신규 패키지 생성 (stdio MCP shim)
- `client.ts` — 얇은 HTTP 클라이언트 (4개 메서드, fetchImpl DI)
- `tools/index.ts` — 4개 tool 정의 (plain JSON Schema inputSchema)
- `src/index.ts` — StdioServerTransport + Server, 4개 tool 등록
- MCP smoke 12/12 pass (in-process fetch 방식)
- `pnpm -r typecheck && pnpm -r build` 전 패키지 통과
- `tools/list` stdio e2e 검증: 4개 tool 올바른 JSON Schema로 노출 확인
- ARCHITECTURE.md: `(선택)` 제거 + MCP stdio 흐름 1줄 추가
- ADR-011: 구현 완료 note 추가

**변경된 파일:**
- `packages/server/src/routes/reads.ts` — 엔드포인트 2개 추가
- `packages/server/src/routes/__tests__/smoke.ts` — 검사 6건 추가
- `docs/ARCHITECTURE.md` — mcp-server 주석 갱신 + 흐름 1줄
- `docs/ADR.md` — ADR-011 clarification note

**신규 파일:**
- `packages/mcp-server/package.json`
- `packages/mcp-server/tsconfig.json`
- `packages/mcp-server/src/index.ts` (stdio entry)
- `packages/mcp-server/src/client.ts` (REST wrapper)
- `packages/mcp-server/src/tools/index.ts` (4개 tool)
- `packages/mcp-server/src/__tests__/smoke.ts`

**Claude Desktop 설정 예시:**
```json
// ~/Library/Application Support/Claude/claude_desktop_config.json
{
  "mcpServers": {
    "analyze-codebase": {
      "command": "node",
      "args": ["/path/to/codebase-analysis/packages/mcp-server/dist/index.js"],
      "env": { "ANALYZE_SERVER_URL": "http://localhost:3000" }
    }
  }
}
```

**다음 단계 (Session 8):**
- OQ-005 최소 웹 UI (vanilla HTML + Hono 정적 서빙)

**참조:**
- ADR-011 MCP 읽기 전용 tool, ADR-013 MCP SDK, ADR-020 HTTP 프락시, ADR-007 읽기 인증 없음, ADR-002 tree-sitter name-match
---
## [Session 7 — E2E Dogfooding] codebase-analysis 자체 레포 검증 완료

**완료한 작업:**
- 스테일 `.js` 아티팩트 11개 삭제 (`packages/server/src/` 내 `.ts` 대응 파일 있는 것)
- `.gitignore`에 `.codebase-analysis/` 추가
- FTS5 검색 버그 수정: `MATCH 'pack*'` → `MATCH 'name:pack*'` (경로 컬럼 오염 방지)
- `q` 파라미터 regex 유효성 검사 추가 (`/^[A-Za-z0-9_]+$/`)
- routes smoke 51 → 54 checks (path-prefix no-hit, injection→400 케이스 추가)
- docker-compose 재빌드, 클린 볼륨으로 463 symbols / 35 files push (index_id=1)
- 멱등성(409 skip), Web UI 정적, REST 5종 검색, body/refs/file-overview, MCP 4 tool, 에러 경로 전부 수동 검증

**발견된 회귀:**
- BUG-1: stale `.js` → 중복 심볼 (삭제로 해결)
- BUG-2: FTS5 전 컬럼 MATCH → file_path 오염 (name: 스코프로 해결)

**검증 수치:**
- 463 symbols / 35 files (stale .js 제거 후)
- smoke 54/54 pass
- MCP: 4/4 tool 응답 정상, 404→-32603 에러 매핑 확인

**다음 단계 (Session 8):**
- `pnpm test` 통합 (각 패키지 test 스크립트 + 루트 pnpm -r test)
- OQ-006 레포 구조 확정, OQ-004 B-variant skeleton

**참조:**
- ADR-012 FTS5 prefix (name: 스코프 적용), ADR-010 멱등성, ADR-002 name-match caveat
---
